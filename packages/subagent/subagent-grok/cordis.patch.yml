# Optional Profile Bundle: register the Grok CLI provider on the Host plane.
# Agent Presets grant the model-facing tool independently.
- insert:
    - id: subagent-grok
      name: '@deepseek-ai/dsh-subagent-grok'
